import java.io.*;
import java.util.*;

public class A {

	public static void main(String[] args) throws Exception {
		// // write your code here
		// Scanner sc = new Scanner(System.in);
		// int t = sc.nextInt();
		// int case1 = 1;
		// while (case1 <= t) {
		// 	int n = sc.nextInt();
		// 	int k = sc.nextInt();
		// 	System.out.println("Debug:" + n + "," + k);
		// 	String str = sc.nextLine();

		// 	int diff = 0;

		// 	for (int i = 0; i < n / 2; i++) {
		// 		if (str.charAt(i) == str.charAt(n - 1 - i))
		// 			diff++;
		// 	}
		// 	if (n % 2 != 0)
		// 		diff++;

		// 	int res = (k - diff > 0) ? (k - diff) : 0;
		// 	System.out.print("Case #" + case1 + ": " + res);
		System.out.println("hola!");
	}
}
